// Tu tem isso
export default interface Theme {
    algo: string;
    alguma: string;
    outra: string;
    fulano: string;
}

// Atualiza para

export default interface Theme {
    algo: string;
    alguma: string;
    outra: string;
    fulano: string;
    icon: string;
}